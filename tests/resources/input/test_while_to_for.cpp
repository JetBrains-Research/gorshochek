#include <bits/stdc++.h>
using namespace std;

void a(int *x) {
    cout << "hello";
    int f;
    while(1) {
        f = 5;
    }
    int test_val = 0;
    while(f < test_val)
        f++;
}

int main(void) {
    int x = 6;
    a(&x);
    while(1);
    int k = 0;
    while(k);
    return 0;
}